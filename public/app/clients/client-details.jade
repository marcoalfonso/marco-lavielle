.container
	h1 {{client.title}}
	.row
		.col-md-6
			h3(ng-show="client.featured") Featured
			h3 Published on {{client.published | date}}

		.col-md-6
			.panel.panel-primary
				.panel-heading Tags
				.panel-body
					div(ng-repeat="tag in client.tags") {{tag}}